export interface NavItem {
  label: string;
  href: string;
}

export interface PropertyDetails {
  beds: number;
  baths: number;
  sqft: number;
  title: string;
  subtitle: string;
  price: number;
  imageUrl: string;
  callNumber: string;
}
